- case_name: demo
  request:
    interface_name: login # 接口后缀
    method: post # 接口请求方法
    parmars: {"username":"admin","password":"123456"} # 接口参数
    headers: # 接口的请求头
  extract: {"token":token} # 获取接口返回值中的变量
  validate: # 验证结果
    msg: success
    httpstatus: 200
    age: 18
    ciyt: changsha
- case_name: demo2
  request:
    interface_name: demo # 接口后缀
    method: get # 接口请求方法
    parmars: # 接口参数
    headers: {"token":"${{token}}"} # 接口的请求头，这里代表token需要从文件中获取（参数化）
  validate: # 验证结果
    from: cemaxueyuan
    name: hello,yideng
    httpstatus: select * from `test`.test
